// official fc SliverApp Bar height
import '../fc_exporter.dart';

const double sliverAppBarHeight = 80;

///This is the height of all bottom sections of the app bars in this fc app
const fcAppbarBottomHeight = kToolbarHeight * 2.5;
const fcAppbarHeight = kToolbarHeight * 1.5;

// drawer width on desktop (when open)
const double sideBarDesktopWidth = 300;

// drawer width on tablet when closed
const double sideBarTabletWidth = 90;
